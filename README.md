# NodeJs-Express-Angular-JEST
A small application to test NodeJS and express framework. 
I also went through trying jest(a javascript testing framework) to test the functions whether they're passed or not.
(Needs some updates)
